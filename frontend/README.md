# GRIM — frontend

Vite + React + TypeScript client for Garima Realty inventory and listings.

## Scripts

- `npm run dev` — dev server (proxies `/api` to the backend)
- `npm run build` — production build
- `npm run test` — Vitest

See the repository root for backend setup and environment notes.
